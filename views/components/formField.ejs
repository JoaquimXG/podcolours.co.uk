<div class="form-field <%= locals.divClass %>">
    <input
        name="<%= id %>"
        id="<%= id %>"
        placeholder="<%= placeholder %>"
        <% if (locals.type) { %>
            type="<%= type %>"
        <% } %>
        <% if (locals.required) { %>
            required
        <% } %>
        class="form-field__input <%= locals.fieldClass %>"
    />
    <label for="<%= id %>" class="form-field__label">
        <%= placeholder %>
    </label>
</div>
